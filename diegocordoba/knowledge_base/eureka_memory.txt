
[2026-06-01 12:15:40] June 1 2026 — Diego entered SNXX/SNDK trade today (Monday). Locked position after intraday dip. Chose to hold through after hours deliberately, comfortable with overnight risk. Thesis: SNXX momentum is structurally strong, company not going down anytime soon. VIX regime: LOW. SNXX entry ~239 area, SNDK ~1765 area. schd_position_pct = 0.61 as hedge leg already active.

[2026-06-18 08:48:39] SNXX es un ETF/ETP apalancado 2x LONG sobre SNDK (SanDisk). Diego tiene posición en SNXX. Su tesis: el momentum del día se escribe en pre-market; quiere señal de timing pre-market + sizing por convicción (Kelly fraccionario). Problema emocional documentado: regret-aversion por vender antes de una baja y que bajara más. NO volver a preguntar qué es SNXX.

[2026-06-21 18:36:42] SINGLE TODO for next Eureka session (logged Sun Jun 21, ~2AM): Wire is_robust flag into Kelly sizing. CONTEXT: is_robust is read (snxx_hedge_signals.py:73) and injected into ENGINE_CONFIG (line 103) but NEVER consumed downstream — 'robust_multiplier' has 0 matches in codebase. Currently is_robust=false (half-life CI 717→2257, fragile) but engine takes NO defensive action on MODEL risk. Regime-switch only handles MARKET risk (VIX), not model risk. FIX (~15 min): after Kelly weights computed, add `robust_mult = 1.0 if config.get('is_robust', True) else 0.5; kelly_pct *= robust_mult`. Add test in stress_test_regime_sizing.py asserting sizing halves when is_robust=false. Confirm multiplicative interaction with regime multiplier. RISK IF SKIPPED: LOW — NaN stop_loss is dead + test-locked (test_nans.py, test_atr.py), Kelly capped at 15%. This is robustness refinement, not a capital leak. Diego cleared to fully relax tonight — earned it.
